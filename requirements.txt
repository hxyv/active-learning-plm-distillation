# Core numerical stack
numpy
scipy
pandas
scikit-learn
matplotlib
pyyaml
tqdm
requests

# Protein parsing and DSSP
biopython
mdtraj==1.10.1

# Deep learning stack (install torch + PyG extensions with CUDA wheels separately)
# pip install torch --index-url https://download.pytorch.org/whl/cu128
# pip install torch-scatter torch-cluster -f https://data.pyg.org/whl/torch-2.10.0+cu128.html
torch
torch-geometric
torch-scatter
torch-cluster

# ESM3 teacher wrapper
esm

# Experiment tracking
wandb
